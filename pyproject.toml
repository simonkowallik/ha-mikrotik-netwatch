[project]
name = "ha-mikrotik-netwatch"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.8.0",
]

[dependency-groups]
dev = [
    "homeassistant>=2024.12.5",
    "ruff>=0.12.7",
]
